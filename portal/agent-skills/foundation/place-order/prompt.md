This skill is deterministic — no LLM prompt is sent. The SKILL.md body documents
the full two-phase contract the dispatcher implements. This file exists so the
skill registry can expose a consistent file layout across skills.
